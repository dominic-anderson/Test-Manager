# Test-Manager
An application to help with CI/CD automation

## Requirements:
- Python 3.13.x (Older versions not tested, newer versions break functionality)

## Arguments:
- `-h`, `--help`: Display all available arguments and their descriptions
- `-v`, `--verbose`: Show verbose output (default: false)